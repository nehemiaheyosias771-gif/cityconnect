rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Your Firestore security rules here
    match /{document=**} {
      allow read, write: if false;
    }
  }
}